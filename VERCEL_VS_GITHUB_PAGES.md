# GitHub Pages vs Vercel 部署差異診斷

## 📊 當前狀態

| 部署平台 | 中文支持 | 代碼版本 | 自動更新 |
|---------|--------|--------|--------|
| GitHub Pages | ✅ 有中文 | 最新 (5594c32) | ✅ 自動 |
| Vercel | ❌ 仍是英文 | 舊版本 (1ea0163) | ⚠️ 需要觸發 |

## 🔍 差異原因分析

### GitHub Pages (✅ 工作正常)
- **自動化**: 通過 `.github/workflows/deploy.yml` 自動部署
- **觸發條件**: 每次推送到 `main` 分支時自動重新構建
- **構建時間**: ~2-3 分鐘
- **構建命令**: `npm run build` (在 GITHUB_PAGES=true 環境下)
- **代碼同步**: 立即同步最新提交

### Vercel (❌ 尚未同步)
- **自動化**: GitHub 推送後需要等待 Vercel 的 webhook 觸發
- **觸發條件**: 依賴於 Vercel GitHub 應用的 webhook 配置
- **構建時間**: 通常 1-3 分鐘，但可能有延遲
- **構建命令**: `npm run build` (在 vercel.json 中配置)
- **代碼同步**: 通常自動，但有時需要手動觸發

## 🚀 解決方案

### 方案 1: 等待 Vercel 自動部署（推薦）
- Vercel 的 webhook 通常在 5-10 分鐘內觸發
- 查看 https://vercel.com/dashboard → worldmonitor → Deployments
- 檢查是否有新的構建任務

### 方案 2: 手動觸發 Vercel 重新部署

**通過 Vercel Dashboard:**
1. 訪問 https://vercel.com/dashboard
2. 選擇 worldmonitor 項目
3. 進入 **Deployments** 標籤
4. 找到最新部署（應該是 main 分支）
5. 點擊 **三點菜單** → **Redeploy**
6. 等待構建完成（通常 1-3 分鐘）

**通過 GitHub:**
1. 進行任何小修改並推送
2. 或在 Vercel Dashboard 中點擊 "Redeploy"

### 方案 3: 通過 Vercel CLI 手動觸發

```bash
# 安裝 Vercel CLI (如果還沒安裝)
npm i -g vercel

# 從項目目錄運行
vercel deploy --prod
```

## 🔧 為什麼會有這種延遲？

1. **Vercel GitHub 應用配置**: webhook 可能有延遲或沒有正確配置
2. **構建隊列**: Vercel 可能正在處理其他項目的構建
3. **緩存**: 瀏覽器或 CDN 可能緩存了舊版本

## ✅ 驗證部署

### 檢查 Vercel 是否已更新

在瀏覽器控制台執行:
```javascript
// 檢查當前版本
fetch('/').then(r => r.text()).then(html => {
  const match = html.match(/assets\/index-([a-z0-9]+)\.js/);
  if(match) console.log('Current JS hash:', match[1]);
})
```

或直接查看最新的 JavaScript 檔案名稱:
1. 打開 https://worldmonitor-six.vercel.app/
2. DevTools → Network 標籤
3. 尋找 `index-*.js` 檔案

### 檢查中文翻譯

```javascript
// 在控制台執行
window.i18nDebug.getDebugInfo()

// 應該看到中文選項可用
```

## 📝 預期完整流程

```
提交代碼到 GitHub main
    ↓
GitHub Actions 自動構建 & 部署到 GitHub Pages
    ↓ (同時)
Vercel webhook 被觸發
    ↓ (通常 5-10 分鐘)
Vercel 自動構建最新代碼
    ↓
新版本上線 → 中文支持可用
```

## 🆘 如果 Vercel 仍未更新

1. **檢查構建日誌**: 
   - Vercel Dashboard → Deployments → 點擊最新部署 → View Build Logs
   - 尋找任何錯誤信息

2. **檢查 GitHub 集成**:
   - Vercel Dashboard → Settings → Git → Vercel for GitHub
   - 確認應用已授權並正確配置

3. **強制重新部署**:
   - 通過 Vercel Dashboard 手動觸發 Redeploy
   - 或使用 Vercel CLI: `vercel deploy --prod`

4. **清除瀏覽器緩存**:
   ```javascript
   // 打開 DevTools → Application → Clear site data
   // 或使用快捷鍵: Ctrl+Shift+Delete (Windows) / Cmd+Shift+Delete (Mac)
   ```

## 📋 檢查清單

- ✅ 代碼已推送到 main 分支
- ✅ GitHub Actions 構建成功（查看 GitHub → Actions 標籤）
- ⏳ 等待 Vercel 部署完成（最多 10 分鐘）
- ⚠️ 如果超過 10 分鐘仍未更新，手動觸發重新部署
- ✅ 驗證中文在 Vercel 版本中顯示

## 🎯 時間表

| 時間 | 事件 |
|------|------|
| T+0 | 推送代碼到 GitHub |
| T+2-3 分鐘 | GitHub Pages 部署完成 ✅ |
| T+5-10 分鐘 | Vercel 自動部署完成 (通常) |
| T+30 分鐘 | 如果未完成，需要手動干預 |

---

**當前狀態**: 等待 Vercel 部署同步最新代碼
**預期完成時間**: ~5-10 分鐘
**建議檢查時間**: 10 分鐘後

如果 15 分鐘後 Vercel 仍未更新，請手動觸發重新部署。
